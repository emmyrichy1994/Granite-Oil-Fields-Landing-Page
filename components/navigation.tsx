'use client';
import Link from 'next/link';
import { AnimatePresence, motion } from 'framer-motion';
import { Menu, X, ChevronRight } from 'lucide-react';
import { useEffect, useState } from 'react';
const links = [{href:'/',label:'Home'}, {href:'/contact',label:'Contact Us'}, {href:'/about',label:'About Granite'}, {href:'/services',label:'Our Services'}, {href:'/careers',label:'Careers'}, {href:'/faq',label:'FAQ'}];
export function Logo() { return <Link className="logo" href="/"><span className="mark">G</span><span>GRANITE<small>OIL FIELDS</small></span></Link>; }
export function Navigation() { const [open,setOpen]=useState(false); useEffect(()=>{const key=(e:KeyboardEvent)=>e.key==='Escape'&&setOpen(false); window.addEventListener('keydown',key); return()=>window.removeEventListener('keydown',key)},[]); return <><header><Logo/><button className="menuButton" onClick={()=>setOpen(true)} aria-label="Open navigation menu"><span>MENU</span><Menu size={29}/></button></header><AnimatePresence>{open&&<><motion.button aria-label="Close navigation menu" className="scrim" onClick={()=>setOpen(false)} initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}}/><motion.aside className="drawer" initial={{x:'100%'}} animate={{x:0}} exit={{x:'100%'}} transition={{type:'tween',ease:[.22,1,.36,1],duration:.45}} aria-label="Navigation"><button className="drawerClose" onClick={()=>setOpen(false)} aria-label="Close navigation menu"><X/></button><Logo/><nav>{links.map((link,i)=><motion.div key={link.href} initial={{opacity:0,x:22}} animate={{opacity:1,x:0}} transition={{delay:.12+i*.05}}><Link href={link.href} onClick={()=>setOpen(false)}>{link.label}<ChevronRight size={18}/></Link>{link.label==='Our Services'&&<div className="subnav"><Link href="/services#drilling">Drilling</Link><Link href="/services#completions">Completions</Link><Link href="/services#frac">Frac Monitoring</Link><Link href="/services#plugging">Well Plugging</Link></div>}</motion.div>)}</nav><p>EXCELLENCE BELOW THE SURFACE</p></motion.aside></>}</AnimatePresence></>; }

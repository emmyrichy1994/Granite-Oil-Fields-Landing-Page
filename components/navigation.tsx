'use client';
import Link from 'next/link';
import { AnimatePresence, motion } from 'framer-motion';
import { Menu, X, ChevronRight, Mail, ArrowUp } from 'lucide-react';
import { useEffect, useState } from 'react';
const links = [{href:'/',label:'Home'}, {href:'/contact',label:'Contact Us'}, {href:'/about',label:'About Granite'}, {href:'/services',label:'Our Services'}, {href:'/careers',label:'Careers'}, {href:'/faq',label:'FAQ'}];
export function Logo() { return <Link className="logo" href="/"><span className="mark">G</span><span>GRANITE<small>OIL FIELDS</small></span></Link>; }
export function Navigation() { const [open,setOpen]=useState(false); const [loading,setLoading]=useState(true); const [showTop,setShowTop]=useState(false); useEffect(()=>{const key=(e:KeyboardEvent)=>e.key==='Escape'&&setOpen(false); const scroll=()=>setShowTop(window.scrollY>450); const timer=window.setTimeout(()=>setLoading(false),650); window.addEventListener('keydown',key); window.addEventListener('scroll',scroll,{passive:true}); scroll(); return()=>{window.removeEventListener('keydown',key);window.removeEventListener('scroll',scroll);window.clearTimeout(timer)}},[]); return <><AnimatePresence>{loading&&<motion.div className="pagePreloader" initial={{opacity:1}} exit={{opacity:0}} transition={{duration:.4}} aria-label="Loading page"><div><span className="preloaderMark">G</span><strong>GRANITE</strong><i/></div></motion.div>}</AnimatePresence><header><Logo/><button className="menuButton" onClick={()=>setOpen(true)} aria-label="Open navigation menu"><span>MENU</span><Menu size={29}/></button></header><AnimatePresence>{open&&<><motion.button aria-label="Close navigation menu" className="scrim" onClick={()=>setOpen(false)} initial={{opacity:0}} animate={{opacity:1}} exit={{opacity:0}}/><motion.aside className="drawer" initial={{x:'100%'}} animate={{x:0}} exit={{x:'100%'}} transition={{type:'tween',ease:[.22,1,.36,1],duration:.45}} aria-label="Navigation"><button className="drawerClose" onClick={()=>setOpen(false)} aria-label="Close navigation menu"><X/></button><Logo/><nav>{links.map((link,i)=><motion.div key={link.href} initial={{opacity:0,x:22}} animate={{opacity:1,x:0}} transition={{delay:.12+i*.05}}><Link href={link.href} onClick={()=>setOpen(false)}>{link.label}<ChevronRight size={18}/></Link>{link.label==='Our Services'&&<div className="subnav"><Link href="/services#drilling" onClick={()=>setOpen(false)}>Drilling</Link><Link href="/services#completions" onClick={()=>setOpen(false)}>Completions</Link><Link href="/services#frac" onClick={()=>setOpen(false)}>Frac Monitoring</Link><Link href="/services#plugging" onClick={()=>setOpen(false)}>Well Plugging</Link></div>}</motion.div>)}</nav><p>EXCELLENCE BELOW THE SURFACE</p></motion.aside></>}</AnimatePresence><a className="emailWidget" href="mailto:info@graniteoilfieldsinc.com" aria-label="Email Granite Oil Fields"><Mail size={22}/><span>Email us</span></a><AnimatePresence>{showTop&&<motion.button className="scrollTop" onClick={()=>window.scrollTo({top:0,behavior:'smooth'})} aria-label="Scroll to top" initial={{opacity:0,y:12}} animate={{opacity:1,y:0}} exit={{opacity:0,y:12}}><ArrowUp size={20}/></motion.button>}</AnimatePresence></>; }
